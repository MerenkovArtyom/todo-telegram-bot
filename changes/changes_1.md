# changes_1

- Updated `app/bot/handlers/voice.py` to add preview flow, pending state, confirm/cancel callbacks, and error handling for voice tasks.
- Added voice-related message strings in `app/bot/messages.py` for consistent responses.
- Marked completed steps in `todo.md` (manual validation left unchecked).
